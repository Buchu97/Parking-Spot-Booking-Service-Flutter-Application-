// Correct placement of buildscript for classpath dependencies
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Classpath for Google Services plugin should be here
        classpath 'com.google.gms:google-services:4.4.1'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = '../build'

subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
    project.evaluationDependsOn(':app')
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
